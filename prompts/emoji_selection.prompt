你需要根据上下文和当前语气，从以下表情包中选择一个最合适的来发送。

候选表情包（编号从 1 到 {emoji_count}）：
{candidate_list}

当前对话情绪提示：{emoji_hint}
最近对话：{recent_context}

你必须返回一个合法的 JSON 对象，格式严格如下：
{{"emoji_index":1,"reason":"简短理由"}}

格式要求：
- emoji_index：选中的表情包编号（1到{emoji_count}），纯数字，不要加引号
- reason：选择理由（1句话），必须用双引号包裹
- reason 内部如需引用词语，用书名号「」或单引号''，不要用双引号"
- 只输出 JSON，不要输出其他任何内容
