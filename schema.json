{
  "steal_emoji": {
    "type": "switch",
    "name": "Steal emojis from chat",
    "default": true,
    "hint": "Automatically save market emojis / stickers sent by others into the library and tag them with VLM. Can also be toggled on the plugin page.",
    "locales": {
      "zh": {
        "name": "偷取聊天表情",
        "hint": "自动收藏他人发送的市场表情/表情包并入库存档，由 VLM 自动打标。也可在插件管理页开关。"
      }
    }
  },
  "steal_require_approval": {
    "type": "switch",
    "name": "Stolen emojis require approval",
    "default": true,
    "hint": "When on, stolen emojis land in the library disabled and must be enabled manually on the plugin page before the AI can use them. They are still auto-tagged so you can review descriptions. Can also be toggled on the plugin page.",
    "locales": {
      "zh": {
        "name": "偷取表情需审核",
        "hint": "开启后偷取的表情包入库即为禁用状态，需在插件管理页手动启用后 AI 才会使用；入库后仍会自动打标以便审核。也可在插件管理页开关。"
      }
    }
  },
  "capacity": {
    "type": "integer",
    "name": "Library capacity",
    "default": 500,
    "hint": "Maximum number of emojis kept in the library. When full, least-used entries are evicted first.",
    "locales": {
      "zh": {
        "name": "图库容量上限",
        "hint": "图库最多保留的表情包数量，满额后优先淘汰使用最少的。"
      }
    }
  },
  "candidate_count": {
    "type": "integer",
    "name": "Candidate count",
    "default": 9,
    "hint": "How many candidates are sampled for the selection LLM to choose from on each send.",
    "locales": {
      "zh": {
        "name": "候选数量",
        "hint": "每次发送表情包时采样多少张候选表情包给选图模型挑选。"
      }
    }
  },
  "max_emoji_size_mb": {
    "type": "float",
    "name": "Max stolen emoji size (MB)",
    "default": 5.0,
    "hint": "Stolen emojis larger than this are skipped.",
    "locales": {
      "zh": {
        "name": "偷取单图大小上限（MB）",
        "hint": "超过此大小的表情包不做偷取。"
      }
    }
  },
  "vlm_model": {
    "type": "model_select",
    "name": "Tagging model (vision)",
    "model_type": "llm",
    "default": "",
    "hint": "Model used to tag stolen/uploaded emojis (image + text prompt, vision required). Empty = host default VLM.",
    "locales": {
      "zh": {
        "name": "打标模型（需视觉）",
        "hint": "用于给偷取/上传的表情包打标的模型（图片+文字输入，需要视觉能力）。留空使用系统默认 VLM。"
      }
    }
  },
  "selection_model": {
    "type": "model_select",
    "name": "Selection model (text-only)",
    "model_type": "llm",
    "default": "",
    "hint": "Model used to pick one emoji from candidate descriptions on each send. Text-only task, no vision needed - any chat model works. Empty = host default LLM.",
    "locales": {
      "zh": {
        "name": "选图模型（纯文本）",
        "hint": "发送表情包时从候选描述中挑选一张的模型。纯文本任务，无需视觉能力，任意对话模型皆可。留空使用系统默认 LLM。"
      }
    }
  },
  "emotion_tag_info": {
    "type": "info",
    "name": "Emotion vocabulary",
    "hint": "Emojis are tagged with a fixed vocabulary of 20 emotion labels: 胆怯、无语、调皮、开心、困惑、震惊、傲娇、害羞、温柔、委屈、期待、生气、无辜、撒娇、嫌弃、嘲讽、感谢、安慰、悲伤、欢迎。 The <sticker_plus> tag asks the AI to pick one of them as the emotion keyword.",
    "level": "info",
    "locales": {
      "zh": {
        "name": "情绪标签词表",
        "hint": "表情包使用固定 20 类情绪标签打标：胆怯、无语、调皮、开心、困惑、震惊、傲娇、害羞、温柔、委屈、期待、生气、无辜、撒娇、嫌弃、嘲讽、感谢、安慰、悲伤、欢迎。<sticker_plus> 标签让 AI 从中选一个情绪关键词。"
      }
    }
  }
}
